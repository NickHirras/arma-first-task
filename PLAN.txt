TASKS
-----

1.  Bulid a simple node.js website with a Header, Title, and Body

   A) Install node.js (homebrew, nodejs, npm)

   B) Verify installation (npm -v, node -v, etc.)


2.  Upgrade website to an MVC model

   A) Import vue.js libraries

   B) Implement routing to serve index template on GET /.

   C) Change the index.html page to use vue for templating.


3. Add a form with Header, Title, and Body input fields that
   updates on a post.

   A) Add a form to the page, with input field for NAME.

   B) Add a route for POST to receive the field, displaying the
      submitted value on the page as "Hello {NAME}", and populating
      that value into the input field.


   
